# Linux 配置管理器 - Tauri 版本

这是 Linux 配置管理器的 Tauri 桌面应用版本，使用 Rust 后端和 React 前端。

## 🚀 技术栈

### 前端
- **框架**: React 19 + TypeScript
- **构建工具**: Vite
- **包管理器**: pnpm
- **UI框架**: Tailwind CSS
- **图标库**: Lucide React
- **状态管理**: React Context + useReducer

### 后端
- **语言**: Rust
- **框架**: Tauri 2.x
- **异步运行时**: Tokio
- **序列化**: Serde
- **文件系统**: std::fs + dirs crate
- **日期时间**: Chrono

## 📦 安装和使用

### 前置要求
- Node.js 18+
- Rust 1.77.2+
- pnpm
- 系统依赖（Linux）:
  - `libwebkit2gtk-4.0-dev`
  - `build-essential`
  - `curl`
  - `wget`
  - `libssl-dev`
  - `libgtk-3-dev`
  - `libayatana-appindicator3-dev`
  - `librsvg2-dev`

### Ubuntu/Debian 安装系统依赖
```bash
sudo apt update
sudo apt install libwebkit2gtk-4.0-dev \
    build-essential \
    curl \
    wget \
    libssl-dev \
    libgtk-3-dev \
    libayatana-appindicator3-dev \
    librsvg2-dev
```

### 快速开始

1. **克隆项目**
   ```bash
   git clone https://github.com/s0raLin/linux-config-manager.git
   cd linux-config-manager
   ```

2. **安装前端依赖**
   ```bash
   pnpm install
   ```

3. **开发模式运行**
   ```bash
   pnpm tauri:dev
   ```

4. **构建生产版本**
   ```bash
   pnpm tauri:build
   ```

### 可用脚本

- `pnpm tauri:dev` - 启动开发模式（热重载）
- `pnpm tauri:build` - 构建生产版本
- `pnpm dev:tauri` - 仅启动前端开发服务器（Tauri配置）
- `pnpm build:tauri` - 仅构建前端（Tauri配置）

## 🎯 功能特性

### 桌面应用优势
- **原生性能**: 使用 Rust 后端，性能优异
- **系统集成**: 更好的系统文件访问权限
- **离线使用**: 无需网络连接
- **安全性**: 沙盒环境，安全可靠
- **跨平台**: 支持 Linux、macOS、Windows

### 核心功能
- **配置文件管理**: 支持常见的 Linux 配置文件
- **实时编辑**: 语法高亮、行号显示
- **撤销/重做**: 完整的编辑历史
- **文件备份**: 一键创建配置文件备份
- **多文件编辑**: 同时编辑多个配置文件
- **自动保存**: 防止数据丢失

### 支持的配置文件类型
- **Shell 配置**: `.bashrc`, `.zshrc`, `.profile`
- **Git 配置**: `.gitconfig`
- **编辑器配置**: `.vimrc`
- **SSH 配置**: `~/.ssh/config`

## 🔧 Tauri 命令 API

### 配置分类
- `get_categories()` - 获取所有配置分类

### 配置文件
- `get_files()` - 获取所有配置文件列表
- `get_file(file_id: String)` - 获取单个配置文件内容
- `update_file(file_id: String, content: String)` - 更新配置文件内容
- `backup_file(file_id: String)` - 创建配置文件备份

### 系统信息
- `get_system_info()` - 获取系统信息

## 📁 项目结构

```
├── src-tauri/                 # Rust 后端代码
│   ├── src/
│   │   ├── commands.rs        # Tauri 命令处理器
│   │   ├── models.rs          # 数据模型
│   │   ├── services.rs        # 业务逻辑服务
│   │   ├── lib.rs            # 库入口
│   │   └── main.rs           # 应用入口
│   ├── Cargo.toml            # Rust 依赖配置
│   └── tauri.conf.json       # Tauri 应用配置
├── components/               # React 组件
│   ├── TauriTopNavbar.tsx    # Tauri 版顶部导航
│   ├── TauriSidebar.tsx      # Tauri 版侧边栏
│   ├── TauriEditor.tsx       # Tauri 版编辑器
│   └── TauriStatusBar.tsx    # Tauri 版状态栏
├── contexts/
│   └── TauriAppContext.tsx   # Tauri 版应用上下文
├── services/
│   └── tauri-api.ts          # Tauri API 服务
├── TauriApp.tsx              # Tauri 版主应用组件
├── tauri-index.tsx           # Tauri 版入口文件
├── tauri.html                # Tauri 版 HTML 模板
└── vite.tauri.config.ts      # Tauri 版 Vite 配置
```

## 🔒 安全特性

- **文件系统访问控制**: 只能访问用户主目录下的配置文件
- **沙盒环境**: Tauri 提供的安全沙盒
- **权限管理**: 明确的文件读写权限
- **备份机制**: 自动创建带时间戳的备份文件

## 🚀 性能优化

- **Rust 后端**: 高性能的系统级操作
- **异步处理**: 使用 Tokio 异步运行时
- **内存安全**: Rust 的内存安全保证
- **小体积**: 编译后的应用体积较小

## 🔮 与 Web 版本的区别

| 特性 | Web 版本 | Tauri 版本 |
|------|----------|------------|
| 部署方式 | 需要后端服务器 | 独立桌面应用 |
| 性能 | 依赖网络 | 原生性能 |
| 文件访问 | 受限 | 完整权限 |
| 离线使用 | 不支持 | 完全支持 |
| 系统集成 | 有限 | 深度集成 |
| 安装方式 | 浏览器访问 | 系统安装包 |

## 🛠️ 开发指南

### 添加新的配置文件类型

1. 在 `src-tauri/src/services.rs` 中的 `get_common_config_files` 函数添加新文件定义
2. 在前端 `components/TauriEditor.tsx` 中的 `getFileLanguage` 函数添加语法高亮支持

### 添加新的 Tauri 命令

1. 在 `src-tauri/src/commands.rs` 中定义新命令
2. 在 `src-tauri/src/lib.rs` 中注册命令
3. 在 `services/tauri-api.ts` 中添加前端调用方法

## 📝 许可证

MIT License - 详见 LICENSE 文件

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📞 支持

如有问题，请在 GitHub 上创建 Issue。